name = "weather-data"
compatibility_date = "2024-01-01"

[build]
command = "apt-get update && apt-get install -y openjdk-17-jdk maven && mvn clean package -DskipTests"

[build.environment]
JAVA_HOME = "/usr/lib/jvm/java-17-openjdk-amd64"

[site]
bucket = "target"